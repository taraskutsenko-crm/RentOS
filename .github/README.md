# .github

## Workflows

- [`workflows/ci.yml`](workflows/ci.yml) — runs on every push/PR to `main`:
  install dependencies, build, lint, and type-check the whole workspace.

Issue and pull request templates are not configured yet.
